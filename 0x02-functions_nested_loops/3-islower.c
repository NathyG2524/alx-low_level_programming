#include "main.h"
/**
 *shows 1 if the input is a lowercse
 *else shows  0
 */
int_islower(int c)
{
  if (c >= 97 && c <= 122)
    {
      return (1);
    }
  else
    {
      return (0);
    }
  _putchar('/n');
}
